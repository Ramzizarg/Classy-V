"use client";

/**
 * Shop stays black / white storefront-wide (home, collection, PDP).
 * Kept mounted so layout imports stay stable; no theme class is applied.
 */
export function ProductPageThemeSync() {
  return null;
}
